SELECT
    C.ID AS ID,
    C.GENOTYPE AS GENOTYPE,
    P.GENOTYPE AS PARENT_GENOTYPE
FROM
    ECOLI_DATA C JOIN ECOLI_DATA P ON C.PARENT_ID = P.ID
WHERE
    C.GENOTYPE & P.GENOTYPE = P.GENOTYPE
    -- C.GENOTYPE | P.GENOTYPE = C.GENOTYPE
ORDER BY
    ID ASC